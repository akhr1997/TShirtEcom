import React from 'react';
import Base from "../core/Base"

const UserDashboard = () => {
    return (
        <Base title="User Dashboard" description="Welcome to User Dashboard of Tee-Store">
        <h1>Welcome to User Dashboard of Tee-Store</h1>
            
        </Base>
    );
}

export default UserDashboard;